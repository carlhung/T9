import XCTest
@testable import T9

final class T9Tests: XCTestCase {
    func testExample() throws {
        let t9 = try T9()

        let com: [Character] = ["u","j"]
        // t9.wordList.filter({ word in
        //     com.forEach { char in
        //         word.combination.
        //     }
        // })

        // XCTAssertEqual(noThrow, true)
    }

    func testExample1() throws {

    }
}
